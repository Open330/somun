# 서버
PORT=8790
DATA_DIR=./data                 # SQLite 파일 위치 (somun.db)
LOG_LEVEL=info

# 인증. 셋 중 하나:
#  1) 로컬 개발: SOMUN_ALLOW_ANONYMOUS=true
#  2) 단일 사용자: SOMUN_TOKEN=긴-비밀문자열  (Authorization: Bearer)
#  3) jiun-api: AUTH_ISSUER=https://api.jiun.dev AUTH_JWKS_URL=https://api.jiun.dev/.well-known/jwks.json AUTH_AUDIENCE=somun
SOMUN_ALLOW_ANONYMOUS=true

# 웹에서 GitHub App 최초 등록을 허용할 운영자. 비우면 등록 비활성화. 익명 인증은 허용하지 않음.
# SOMUN_ADMIN_OWNER_ID=local  # 공유 토큰 기본 ownerId. JWT는 /api/me의 ownerId를 사용.
# SOMUN_PUBLIC_URL=https://somun.example.com  # 리버스 프록시 환경의 공개 주소

# 수집·LLM
GITHUB_TOKEN=
GEMINI_API_KEYS={"free-1":"..."}   # 라벨 붙은 JSON 맵. paid-* 는 순환하지 않음
CRON=0 0 * * *                     # UTC. 기본 매일 09:00 KST

# 웹 (빌드 시)
VITE_AUTH_URL=                     # jiun-api OAuth 화면. 없으면 로그인 UI 숨김
VITE_AUTH_AUDIENCE=somun

# 저장하는 비밀값(BYOK API 키, Discord 웹훅 URL, GitHub App 개인키) 암호화 키. 16자 이상 임의 문자열.
# 비우면 평문으로 저장한다. 한 번 정하면 바꾸지 말 것(바꾸면 저장된 값을 열 수 없어 서버가 시작하지 않는다).
# 만들기: openssl rand -base64 32
SOMUN_SECRET_KEY=

# GitHub App의 OAuth client. 앱 설정에서 "Request user authorization (OAuth) during installation"을 켜고
# Callback URL을 <공개 주소>/github/setup으로 두면, 설치를 처음 연결할 때 그 GitHub 사용자가 설치에 접근할 수 있는지 확인한다.
# 없으면 운영자만 설치를 연결할 수 있다.
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
